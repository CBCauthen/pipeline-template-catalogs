pipeline {
    agent any
    stages {
        stage('Build') {
          steps {
            echo "${previewEnvHostname}"
            echo "${emailRecipient}"
            sh "echo ./mvnw clean install"
          }
        }
    }
}
